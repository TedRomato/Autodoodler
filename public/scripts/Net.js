class Net{

  constructor(){

    this.nodes = [];

  }

  drawSelftToCanvas(nodeOptions = {colors:["#000000"], scale:1}, connectLineOptions = {colors:["#000000"], width:4}, xUnitInPixels, yUnitInPixels, context){
    //iterate through nodes
    //  if two nodes are connected
    //  and connection is already not drawn (was not drawn in previous iteration from the other node)
    //  draw line between nodes

    //draw Each node
  }

}
